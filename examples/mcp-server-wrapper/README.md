# Experimental: MCP server wrapper

Not SoftStop v0.1. SoftStop gates **user pressure** via `check` / `record` — see [examples/agent-touchpoint](../agent-touchpoint) and [archive/mcp-gateway](../../archive/mcp-gateway).

This folder sketched wrapping MCP tool handlers with a separate authorize helper from `packages/gateway`.
