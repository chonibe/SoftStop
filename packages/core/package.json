{
  "name": "@governor/core",
  "version": "0.0.0",
  "description": "Deterministic pressure / escalation permit engine (experimental package extraction). [NON-CANONICAL — use governor/api]",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "test": "vitest run"
  },
  "license": "MIT"
}
