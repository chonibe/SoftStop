# Local SoftStop API (default when unset: http://localhost:3000)
# Prefer SoftStop naming; GOVERNOR_API_URL remains supported.
SOFTSTOP_API_URL=http://localhost:3000
GOVERNOR_API_URL=http://localhost:3000

# Optional: hosted evaluation endpoint (not required for self-host)
# SOFTSTOP_API_URL=https://softstop.vercel.app
# GOVERNOR_API_URL=https://softstop.vercel.app

# Server port
PORT=3000

# Optional: force in-memory even if Supabase vars exist (local/CI)
# GOVERNOR_STORAGE=memory

# Auth: required when GOVERNOR_STORAGE=supabase (or set SOFTSTOP_AUTH=required).
# Private single-tenant: SOFTSTOP_AUTH=off (memory/dev default).
# SOFTSTOP_AUTH=required
# SOFTSTOP_AUTH=off
# SOFTSTOP_TENANT_ID=default

# Check-and-reserve: production/Supabase defaults ON (20s).
# SOFTSTOP_RESERVE=on
# SOFTSTOP_RESERVE_TTL_MS=20000
# SOFTSTOP_RESERVE=off alone is REFUSED on Supabase — requires escape hatch:
# SOFTSTOP_UNSAFE_LEGACY_CHECK=1
# (warns at startup; unsafe under concurrency — not for production traffic)

# Policy pack (optional). Precedence: *_POLICY_FILE > *_POLICY preset > builtin defaults.
# Presets: default | strict | lenient  (files in policies/)
# SOFTSTOP_POLICY=default
# SOFTSTOP_POLICY=strict
# SOFTSTOP_POLICY_FILE=./policies/lenient.json
# GOVERNOR_POLICY=lenient
# GOVERNOR_POLICY_FILE=./policies/default.json

# Optional persistent storage (omit for in-memory)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_ANON_KEY=

# Optional admin API for tenant-scoped keys
GOVERNOR_ADMIN_SECRET=
